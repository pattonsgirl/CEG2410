#! /bin/bash

# function for package installs
# check if root, then install packages silently
# use apt to install: git, vim, python3, ?

# function for vim configuration
# download vim colorscheme (check location per documentation)
# configure `.vimrc` to auto use colorscheme
# install vim plugin - nerdtree AND/OR git plugin?

# function for downloading `authorized_keys` file
# add authorized_keys file to github repo
# download file to `~/.ssh/`

# function that calls all other functions (driver)

